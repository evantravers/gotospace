<p id="notice"><%= notice %></p>

<p>
  <%= @list.name %>:
</p>
<ul>
  <% @todos.each do |todo| %>
    <li><%= todo.name %></li>
  <% end %>
</ul>

<h4><%= link_to 'New Todo', new_list_todo_path(@list) %></h4>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
